services:
  - type: web
    name: falcon-learning-path
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: SQLITE_DB_PATH
        value: ./career_hq.db
