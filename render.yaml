services:
  - type: web
    name: falcon-learning-path
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: OPENAI_API_KEY
        sync: false
      - key: DATABASE_URL
        value: ./career_hq.db
